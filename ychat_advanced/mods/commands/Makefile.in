MODS=$(SRCS:.cpp=.so)
CC=g++
all: mods
$(MODS): $(SRCS)
	$(CC)  -shared -o $* `echo $* | sed s/.so//`.cpp
infotext:
	@echo Compiling command modules
mods:	infotext $(MODS) 
	ls *.so
	ls *.so | wc -l
clean:
	@echo Cleaning command modules
	rm *.so
