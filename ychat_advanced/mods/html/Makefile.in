MODS=$(SRCS:.cpp=.so)
CC=g++
all: mods
$(MODS): 
	$(CC) -shared -o $* `echo $* | sed s/.so//`.cpp
infotext:
	@echo Compiling html modules
mods:	infotext $(MODS) 
	ls *.so
	ls *.so | wc -l
clean:
	@echo Cleaning html modules
	rm *.so 
